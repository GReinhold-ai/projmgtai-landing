export type ProjectData = {
  projectName: string;
  projectType: string;
  location: string;
  startDate: string;
  notes?: string;
  trades: string[];
  plansUrl?: string;
  specsUrl?: string;
  createdAt: string;
};
